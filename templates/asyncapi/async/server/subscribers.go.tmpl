// Don't edit this file, as it is generated by dredger
package subscribers

import (
	"{{ lcfirst ( camelcase .ModuleName ) }}/entities"
	"{{ lcfirst ( camelcase .ModuleName ) }}/core/log"
	"encoding/json"

	"github.com/gorilla/websocket"
	"github.com/nats-io/nats.go"
)

{{- range .Operations }}
{{- $operation := . }}
{{- range .Messages }}
func {{ ucfirst ( camelcase $operation.OperationName) }}(nc *nats.Conn) *nats.Subscription {
	// Subscribe to "{{$operation.ChannelName}}.{{.MessageName}}"
	Sub, err := nc.Subscribe("{{$operation.ChannelName}}.{{.MessageName}}", func(msg *nats.Msg) {
        var data entities.{{ .MessageStructName }}
		err := json.Unmarshal(msg.Data, &data)
  		if err != nil {
 			log.Error().Err(err).Msg("Unmarshalling {{.MessageStructName}} for {{ .MessageName }} in {{ $operation.OperationName}} failded")
 			return
  		}

        // buisness logic
    })
    if err != nil {
  		log.Error().Err(err).Msg("NATS subscribe to {{ $operation.ChannelName}}.{{ .MessageName }} failed")
  		return nil
   	}
   	return Sub
}
{{ end }}
{{ end }}
