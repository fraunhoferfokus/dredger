package pages

import (
	"github.com/nicksnyder/go-i18n/v2/i18n"
)

// the main content of the index page
templ Content(lzr *i18n.Localizer) {
    // enable the lines, if you want to use events for notices and a progress bar
    // <div id="Notice" hx-ext="sse" sse-connect="/events?stream=notice" sse-swap="Notice"></div>
    // <div id="Progress" hx-ext="sse" sse-connect="/events?stream=progress" sse-swap="Progress"></div>
    Hello dredger
}
